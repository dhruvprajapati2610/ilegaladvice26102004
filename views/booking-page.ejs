<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description"
    content="Browse top-rated lawyers and advocates in India. Filter by state, city, expertise, experience, and more. Get expert legal advice today on iLegalAdvice!" />
  <meta name="keywords"
    content="Find lawyers in India, Best advocates in India, Expert legal advice online, Lawyers by state, Experienced lawyers" />
  <meta name="author" content="iLegalAdvice" />
  <title>
    Find the Best Lawyers and Legal Advice in India | iLegalAdvice
  </title>
  <link rel="canonical" href="https://www.ilegaladvice.com/find-lawyers" />
  <link rel="icon" type="image/png" href="../uploads/logo.png" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.1.0/remixicon.css" />

  <link rel="stylesheet" href="booking-page.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css"      />
</head>

<body>
  <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
  <%- include('partials/header') %>

    <div class="body">
      <div class="main-body">
        <div class="booking-container-0">
          <div class="booking-container">
            <div class="booking-heading-container">
              <div class="booking-heading">
                <h2>Book An Appointment</h2>
              </div>
              <div class="booking-para">
                <!-- <% if(!userId){ %>
                  <p style="color: red">
                    Please
                    <a href="/signup" style="text-decoration: underline">Log In</a>
                    First to book an appointment with a lawyer.
                  </p>
                  <% } %> -->
                  <% if(role === "lawyer"){ %>
                    <p>

                      Easily book an appointment with a lawyer through our seamless
                        booking feature. Simply fill out a short form with your
                        details, and our team will promptly contact you for
                        confirmation. No hassle, no long wait times—just a quick and
                        efficient way to connect with the right legal expert. Get the
                        guidance you need, when you need it!
                        <p class="lawyer-warning">Only clients are allowed to book an appointment.</p> 
                    </p>
                    <% }else { %>
                      <p>
                        Easily book an appointment with a lawyer through our seamless
                        booking feature. Simply fill out a short form with your
                        details, and our team will promptly contact you for
                        confirmation. No hassle, no long wait times—just a quick and
                        efficient way to connect with the right legal expert. Get the
                        guidance you need, when you need it!
                      </p>
                      <% } %>
              </div>
            </div>
            <div class="booking-from-container">
              <div class="booking-form">
                <form action="/client-appointment-details" method="post">
                  <% if(userId){ %>
                    <input type="hidden" id="loginCheck" name="userId" value="<%=userId%>" />
                    <% }else { %>
                      <input type="hidden" id="loginCheck" name="userId" value="" />
                           <% } %>
                        <div class="input-fields">
                          <div class="input-field if-field">
                            <input type="text" name="name" id="client-name" placeholder="Your Name" <%= role === "client" ? "" : "disabled" %> />
                          </div>
                          <div class="input-field if-field">
                            <input type="tel" name="phone" id="client-phone" placeholder="Contact number" required <%= role === "client" ? "" : "disabled" %> />
                          </div>
                          <div class="input-field if-field">
                            <input type="email" name="email" id="client-email" placeholder="Your Email" required <%= role === "client" ? "" : "disabled" %> />
                          </div>
                          <div class="input-field if-field">
                            
                            <input type="date" name="date" id="client-date" placeholder="Select prefferd date"
                              required <%= role === "client" ? "" : "disabled" %> />
                          </div>
                          <div class="input-field if-field">
                            <div class="custom-select-g ">
                              <div class="select-button-g <%= role === "client" ? "" : "disabledCustom"%>" role="combobox" aria-labelledby="select button"
                                aria-haspopup="listbox" aria-expanded="false" aria-controls="select-dropdown-2">
                                <span class="selected-value-g" name="city">Select city</span>
                                <span class="arrow-g <%= role === "client" ? "" : "disabledToggle"%>"> </span>
                              </div>
                              <ul class="select-dropdown-g <%= role === "client" ? "" : "disabledDropdown"%> " role="listbox" id="select-dropdown-2" ></ul>
                            </div>
                          </div>
                          <div class="input-field if-field">
                            <div class="custom-select-n <%= role === "client" ? "" : "disabled" %>" style="width: 200px">
                              <select name="areaofpractice" >
                                <option value="" class="select-aop">Select Area of Practice</option>
                                <option value="Divorce">Divorce</option>
                                <option value="Family Dispute">Family Dispute</option>
                                <option value="Child Custody">Child Custody</option>
                                <option value="Medical Negligence">
                                  Medical Negligence
                                </option>
                                <option value="Criminal">Criminal</option>
                                <option value="Property">Property</option>
                                <option value="Cyber Crime">Cyber Crime</option>
                                <option value="Labour & Services">
                                  Labour & Services
                                </option>
                                <option value="Landlord">Landlord</option>
                                <option value="Real Estate">Real Estate</option>
                                <option value="Trademark & copyright">
                                  Trademark & Copyright
                                </option>
                                <option value="Startup">Startup</option>
                                <option value="Custom & Central Excises">
                                  Custom & Central Excises
                                </option>
                                <option value="Banking & Finance">
                                  Banking & Finance
                                </option>
                                <option value="Corporate">Corporate</option>
                                <option value="Gst & Tax">Gst & Tax</option>
                                <option value="Documentation">Documentation</option>
                                <option value="Consumer Court">Consumer Court</option>
                                <option value="Civil">Civil</option>
                                <option value="Check Bounce">Check Bounce</option>
                                <option value="RERA">RERA</option>
                                <option value="Litigation and Dispute Resolution">
                                  Litigation and Dispute Resolution
                                </option>
                                <option value="Insurance">Insurance</option>
                                <option value="Immigration">Immigration</option>
                                <option value="international law">
                                  International Law
                                </option>
                                <option value="Central Administrative Tribunal">
                                  Central Administrative Tribunal
                                </option>
                                <option value="Maharashtra Administrative Tribunal">
                                  Maharashtra Administrative Tribunal
                                </option>
                                <option value="Debts Recovery Tribunal">
                                  Debts Recovery Tribunal
                                </option>
                              </select>
                            </div>
                          </div>
                          <div class="input-field if-field bio">
                            <textarea name="legalIssue" id="legal-issue" cols="30" rows="10"
                              placeholder="Describe your legal issue in simple words" required <%= role === "client" ? "" : "disabled" %>></textarea>
                          </div>

                        </div>
                   
                        <div class="btn-container">
                          <div class="btn-layer"></div>
                          <input 
                          type="submit" 
                          class="submit-btn <%= role === "client" ? "" : "disabledButton"%>"
                          id="lawyer-signup-btn" 
                          <%= role === "client" ? '' : 'disabled' %> 
                        />
                        
                        </div>
                      
                </form>
              </div>
            </div>
          </div>
        </div>
        <% if(previousBookings.length !=0){ %>
          <div class="previous-booking-container-0">
            <div class="previous-booking-container">
              <% if(userId) { %>
                <div class="previous-booking-heading">
                  <h2>Previous Bookings</h2>
                </div>
                <div class="previous-booking-table">
                  <table class="booking-table">
                    <colgroup>
                      <col id="col-1" />
                      <col id="col-2" />
                      <col id="col-3" />
                      <col id="col-4" />
                      <col id="col-5" />
                    </colgroup>

                    <tr>
                      <th>No</th>
                      <th>Applied Date</th>
                      <th>Appointment Date</th>
                      <th>Lawyer Appointed</th>
                      <th>Appointment Status</th>
                    </tr>

                    <% previousBookings.forEach((booking, index)=> { %>
                      <tr>
                        <td>
                          <%= index + 1 %>
                        </td>
                        <td>
                          <%= booking.appliedDate %>
                        </td>
                        <td>
                          <%= booking.appointmentDate %>
                        </td>
                        <td>
                          <div class="lawyer-profile">
                            <div class="lawyer-profile-img-container">
                              <div class="lawyer-profile-img">
                                <% if(booking.lawyerImage !=" " ){ %>
                                  <img src="<%= booking.lawyerImage %>" alt="" />
                                  <% } %>
                              </div>
                            </div>
                            <div class="lawyer-name">
                              <a href="/lawyersprofile?lawyerId=<%=booking.lawyer_id%>">
                                <%=booking.lawyerName %>
                              </a>
                            </div>
                          </div>
                        </td>
                        <td>
                          <%= booking.lawyerAppointedStatus ? "Appointed" : "Pending..." %>
                        </td>
                      </tr>

                      <% }) %>
                  </table>
                </div>
                <% } %>
            </div>
          </div>
          <% } %>
      </div>
      <div class="appointment-popup-container">
          <div class="appointment-popup">
            
          </div>
      </div>
      <%- include('partials/footer') %>
    </div>

    <script>
      const checkUserIsLogin = () => {
        const loginCheck = document.getElementById("loginCheck");

        if (loginCheck === null || loginCheck.value.trim() === "") {
          Toastify({
            text: "Please Login to book an appointment",
            duration: 3000,
            gravity: "top",
            position: "center",
            backgroundColor: "red",
            stopOnFocus: true,
          }).showToast();
        }
      };

      const lawyerToast = () => {
        Toastify({
            text: "Only Clients can book an appointment",
            duration: 3000, 
            gravity: "top", 
            position: "center", 
            backgroundColor: "red", 
            stopOnFocus: true, 
          }).showToast();
      }
      
      window.onload = checkUserIsLogin;
      

      const showmenu = (toggleId, navId) => {
        const toggle = document.getElementById(toggleId),
          nav = document.getElementById(navId);

        toggle.addEventListener("click", () => {
          nav.classList.toggle("show-menu");
          toggle.classList.toggle("show-icon");
        });
      };
      showmenu("nav-toggle", "nav-menu");

      const dropdownItems = document.querySelectorAll(".dropdown-item");

      dropdownItems.forEach((item) => {
        const dropdownButton = item.querySelector(".dropdown-button");

        dropdownButton.addEventListener("click", () => {
          const showDropdown = document.querySelector(".show-dropdown");

          toggleItem(item);

          if (showDropdown && showDropdown !== item) {
            toggleItem(showDropdown);
          }
        });
      });

      const toggleItem = (item) => {
        const dropdownContainer = item.querySelector(".dropdown-container");

        if (item.classList.contains("show-dropdown")) {
          dropdownContainer.removeAttribute("style");
          item.classList.remove("show-dropdown");
        } else {
          dropdownContainer.style.height =
            dropdownContainer.scrollHeight + "px";
          item.classList.add("show-dropdown");
        }
      };
      const mediaQuery = matchMedia("(min-width:1118px)"),
        dropdownContainer = document.querySelectorAll(".dropdown-container");

      const removeStyle = () => {
        if (mediaQuery.matches) {
          dropdownContainer.forEach((e) => {
            e.removeAttribute("style");
          });
          dropdownContainer.forEach((e) => {
            e.classList.remove("show-dropdown");
          });
        }
      };
      addEventListener("resize", removeStyle);

      const customSelects = document.querySelectorAll(".custom-select-1");
      const selectButtons = document.querySelectorAll(".select-button");
      const selectedValues = document.querySelectorAll(".selected-value");
      const optionsLists = document.querySelectorAll(".select-dropdown");
      const isPhoneView = window.matchMedia("(max-width: 640px)").matches;

      customSelects.forEach((customSelect, index) => {
        const selectBtn = selectButtons[index];
        const selectedValue = selectedValues[index];
        const optionsList = optionsLists[index];

        selectBtn.addEventListener("click", () => {
          customSelect.classList.toggle("active");
          selectBtn.setAttribute(
            "aria-expanded",
            selectBtn.getAttribute("aria-expanded") === "true"
              ? "false"
              : "true"
          );
        });

        optionsList.querySelectorAll("li").forEach((option) => {
          function handler(e) {
            if (
              (e.type === "click" && e.clientX !== 0 && e.clientY !== 0) ||
              e.key === "Enter"
            ) {
              selectedValue.textContent = option.children[1].textContent;
              customSelect.classList.remove("active");
            }
          }

          option.addEventListener("keyup", handler);
          option.addEventListener("click", handler);
        });
      });

      var x, i, j, l, ll, selElmnt, a, b, c;
      /* Look for any elements with the class "custom-select": */
      x = document.getElementsByClassName("custom-select-n");
      l = x.length;
      for (i = 0; i < l; i++) {
        selElmnt = x[i].getElementsByTagName("select")[0];
        ll = selElmnt.length;
        /* For each element, create a new DIV that will act as the selected item: */
        a = document.createElement("DIV");
        a.setAttribute("class", "select-selected");
        a.innerHTML = selElmnt.options[selElmnt.selectedIndex].innerHTML;
        x[i].appendChild(a);
        /* For each element, create a new DIV that will contain the option list: */
        b = document.createElement("DIV");
        b.setAttribute("class", "select-items select-hide");
        for (j = 1; j < ll; j++) {
          /* For each option in the original select element,
    create a new DIV that will act as an option item: */
          c = document.createElement("DIV");
          c.innerHTML = selElmnt.options[j].innerHTML;
          c.addEventListener("click", function (e) {
            /* When an item is clicked, update the original select box,
        and the selected item: */
            var y, i, k, s, h, sl, yl;
            s = this.parentNode.parentNode.getElementsByTagName("select")[0];
            sl = s.length;
            h = this.parentNode.previousSibling;
            for (i = 0; i < sl; i++) {
              if (s.options[i].innerHTML == this.innerHTML) {
                s.selectedIndex = i;
                h.innerHTML = this.innerHTML;
                y = this.parentNode.getElementsByClassName("same-as-selected");
                yl = y.length;
                for (k = 0; k < yl; k++) {
                  y[k].removeAttribute("class");
                }
                this.setAttribute("class", "same-as-selected");
                break;
              }
            }
            h.click();
          });
          b.appendChild(c);
        }
        x[i].appendChild(b);
        a.addEventListener("click", function (e) {
          /* When the select box is clicked, close any other select boxes,
    and open/close the current select box: */
          e.stopPropagation();
          closeAllSelect(this);
          this.nextSibling.classList.toggle("select-hide");
          this.classList.toggle("select-arrow-active");
        });
      }

      function closeAllSelect(elmnt) {
        /* A function that will close all select boxes in the document,
  except the current select box: */
        var x,
          y,
          i,
          xl,
          yl,
          arrNo = [];
        x = document.getElementsByClassName("select-items");
        y = document.getElementsByClassName("select-selected");
        xl = x.length;
        yl = y.length;
        for (i = 0; i < yl; i++) {
          if (elmnt == y[i]) {
            arrNo.push(i);
          } else {
            y[i].classList.remove("select-arrow-active");
          }
        }
        for (i = 0; i < xl; i++) {
          if (arrNo.indexOf(i)) {
            x[i].classList.add("select-hide");
          }
        }
      }

      /* If the user clicks anywhere outside the select box,
then close all select boxes: */
      document.addEventListener("click", closeAllSelect);

      const customSelectsG = document.querySelectorAll(".custom-select-g");
      const selectButtonsG = document.querySelectorAll(".select-button-g");
      const selectedValuesG = document.querySelectorAll(".selected-value-g");
      const optionsListsG = document.querySelectorAll(".select-dropdown-g");

      const maharashtraCities = [
        "Airoli",
        "Ambernath",
        "Andheri",
        "Badlapur",
        "Bandra",
        "Belapur",
        "Bhandup",
        "Bhayandar",
        "Bhiwandi",
        "Borivali",
        "Chembur",
        "Colaba",
        "Dadar",
        "Dahisar",
        "Diva",
        "Dombivli",
        "Ghatkopar",
        "Goregaon",
        "Jogeshwari",
        "Juhu",
        "Kalwa",
        "Kalyan",
        "Kandivali",
        "Kanjurmarg",
        "Karjat",
        "Khar",
        "Khopoli",
        "Kurla",
        "Mahalaxmi",
        "Mahim",
        "Malad",
        "Mankhurd",
        "Mira Road",
        "Mulund",
        "Mumbra",
        "Mumbai",
        "Navi Mumbai",
        "Nerul",
        "Panvel",
        "Parel",
        "Powai",
        "Sanpada",
        "Santacruz",
        "Seawoods",
        "Sion",
        "Thane",
        "Turbhe",
        "Ulhasnagar",
        "Vashi",
        "Vikhroli",
        "Vile Parle",
        "Virar",
        "Wadala",
        "Worli"
      ];

      function updateCityDropdown() {
        const cityDropdown = document.querySelector("#select-dropdown-2");
        const citySelectButton = cityDropdown.previousElementSibling;
        const citySelectedValue =
          citySelectButton.querySelector(".selected-value-g");

        cityDropdown.innerHTML = ""; // Clear existing options

        maharashtraCities.forEach((city) => {
          const cityOption = document.createElement("li");
          cityOption.setAttribute("role", "option-g");
          cityOption.innerHTML = `
      <input type="radio" id="${city}" name="city" value="${city}" />
      <label for="${city}">${city}</label>
    `;
          cityDropdown.appendChild(cityOption);

          cityOption.addEventListener("keyup", function (e) {
            if (e.key === "Enter") {
              citySelectedValue.textContent =
                cityOption.children[1].textContent;
              cityDropdown.parentNode.classList.remove("active");
            }
          });

          cityOption.addEventListener("click", function () {
            citySelectedValue.textContent = cityOption.children[1].textContent;
            cityDropdown.parentNode.classList.remove("active");
          });
        });

        citySelectedValue.textContent = "Select city"; // Default text
      }

      function enableCityDropdown() {
        const cityCustomSelect =
          document.querySelector("#select-dropdown-2").parentNode;
        cityCustomSelect.classList.remove("disabled");
      }

      // Enable city dropdown by default
      document
        .querySelector("#select-dropdown-2")
        .parentNode.classList.remove("disabled");

      // Populate cities on page load
      updateCityDropdown();

      customSelectsG.forEach((customSelect, index) => {
        const selectBtnG = selectButtonsG[index];
        const selectedValueG = selectedValuesG[index];
        const optionsListG = optionsListsG[index];

        selectBtnG.addEventListener("click", () => {
          if (!customSelect.classList.contains("disabled")) {
            customSelect.classList.toggle("active");
            selectBtnG.setAttribute(
              "aria-expanded",
              selectBtnG.getAttribute("aria-expanded") === "true"
                ? "false"
                : "true"
            );
          }
        });

        optionsListG.querySelectorAll("li").forEach((option) => {
          function handler(e) {
            if (
              (e.type === "click" && e.clientX !== 0 && e.clientY !== 0) ||
              e.key === "Enter"
            ) {
              selectedValueG.textContent = option.children[1].textContent;
              customSelect.classList.remove("active");
            }
          }

          option.addEventListener("keyup", handler);
          option.addEventListener("click", handler);
        });
      });


     

    </script>
</body>

</html>


</html>